# NuGetReferenceUpdater

Console app that finds all pacakge.config files in your solution and upgrades to a specific version.

Usage:

NuGetReferenceUpdater <package name> <old version> <new version>
